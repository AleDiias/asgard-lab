# URL base da API (apenas para build de produção; em dev usa-se o proxy /api-auth → :3001)
VITE_API_URL=http://localhost:3001

# api-core (leads, etc.): em dev o Vite faz proxy /api-core → :3002; em produção defina a origem pública
# VITE_API_CORE_URL=https://core.seudominio.com
