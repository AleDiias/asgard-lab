import type { IntegrationRecord } from "@/types/core-integrations.types";

/** Linha da tabela de integrações (espelha o registo público da API). */
export type IntegrationListRow = IntegrationRecord;
