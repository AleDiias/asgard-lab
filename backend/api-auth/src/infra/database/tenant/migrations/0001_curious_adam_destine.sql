-- Migração duplicada removida: o esquema inicial de `users` está em `0000_initial_users.sql`.
-- Mantemos o ficheiro e o tag no journal para não quebrar bases que já registaram esta entrada.
SELECT 1;
